<%= form_tag movies_path, :method => :get do %>
  Include:
  <%= hidden_field_tag(:movies_sort, @movies_sort)%>
  <% @all_ratings.each do |rating| %>
    <%= rating %>
    <%= check_box_tag "ratings[#{rating}]", '1', (@selected_ratings.include?(rating)) %>
    <% end %>
  <%= submit_tag 'Refresh' %>
<% end %>