<%= render 'movie', movie: @movie %>

<%= link_to_if(allow.edit?, "Edit", [:edit, @movie]) {} %>
<%= button_to "Publish", publish_movie_path(@movie), method: :put, data: { confirm: "Make this film published?" } if @movie.published? %>
<%= button_to "Delete", @movie,  method: :delete, data: { confirm: "Are you sure?" } %>
<%= link_to "Back to movie list", movies_path %> 
